// import { requestStatusNames } from 'features/requestStatus'
import { 
  requestStatusNames, 
  requestStatusStates,
  selectRequestStatus
} from 'features/requestStatus'
         
import { blue, red } from 'logger'

red('slicePaths', 'hi')
red('slicePaths: requestStatusNames', requestStatusNames)





// const { transactionsFetchStatus } = requestStatusNames

export const slicePaths = {
  
  
  
  criteriaResultsItems: [criteriaResults, items],
  
}

